{
  "name": "@vpn-poc/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/migrate.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vpn-poc/env": "workspace:*",
    "drizzle-orm": "^0.38.4",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "drizzle-kit": "^0.30.6"
  },
  "nx": {
    "tags": [
      "scope:poc-vpn",
      "type:lib"
    ]
  }
}
