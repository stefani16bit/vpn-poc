{
  "name": "@vpn-poc/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite-node --watch src/main.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nestjs/core": "^11.2.1",
    "@vpn-poc/api": "workspace:*",
    "@vpn-poc/env": "workspace:*",
    "@vpn/ports": "^0.16.0",
    "nestjs-pino": "^4.6.1",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@vpn/config": "^0.1.0",
    "@vpn/testing": "^0.18.0",
    "vite-node": "^6.0.0"
  },
  "nx": {
    "tags": [
      "scope:poc-vpn",
      "type:deployment"
    ]
  }
}
