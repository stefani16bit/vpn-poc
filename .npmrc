# @vpn/* comes from the local Verdaccio, everything else from npmjs. This is
# the one line that makes the submodule boundary real at install time.
@vpn:registry=http://localhost:24873

# Nest and Drizzle both rely on reaching transitive peers at runtime.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
